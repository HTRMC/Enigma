#include "World.h"

// World is header-only (all public methods are templates or trivially inlined).
// This translation unit exists to satisfy the build system and provide a single
// place for any future non-template definitions.
